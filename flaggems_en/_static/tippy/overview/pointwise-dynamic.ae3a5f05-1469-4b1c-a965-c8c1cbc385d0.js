selector_to_html = {"a[href=\"#use-the-pointwise-dynamic-decorator\"]": "<h2 class=\"tippy-header\" style=\"margin-top: 0;\">5. Use the <code class=\"docutils literal notranslate\"><span class=\"pre\">@pointwise_dynamic</span></code> decorator<a class=\"headerlink\" href=\"#use-the-pointwise-dynamic-decorator\" title=\"Link to this heading\">#</a></h2><h3>5.1 Basic<a class=\"headerlink\" href=\"#basic\" title=\"Link to this heading\">#</a></h3><p>Decorating the pointwise operator function with <code class=\"docutils literal notranslate\"><span class=\"pre\">pointwise_dynamic</span></code> can save the manual handling of\ntensor addressing, tensor read/write, parallel tiling, tensor broadcasting, dynamic dimensions,\nnon-contiguous storage, type promotion, etc.</p><p>For example, in the following code, you only need to provide a Triton JIT function\ndescribing the computational logic (the Payload), the decorated function can then\ntake torch tesors as inputs and outputs, and support broadcasting, type-promotion, etc.</p>", "a[href=\"#pointwise-dynamic-operators\"]": "<h1 class=\"tippy-header\" style=\"margin-top: 0;\">Pointwise Dynamic Operators<a class=\"headerlink\" href=\"#pointwise-dynamic-operators\" title=\"Link to this heading\">#</a></h1><h2>1. Pointwise operations<a class=\"headerlink\" href=\"#pointwise-operations\" title=\"Link to this heading\">#</a></h2><p>Pointwise operators are trivial to parallelize.\nMost parallel programming guides begin with pointwise addition\nbetween 2 contiguous vectors.\nFor <a class=\"reference external\" href=\"https://triton-lang.org/main/getting-started/tutorials/01-vector-add.html#sphx-glr-getting-started-tutorials-01-vector-add-py\"><code class=\"docutils literal notranslate\"><span class=\"pre\">vector_add</span></code> in Triton</a>,\nit is simple to implement a task partitioning schema that each CTA reads a contiguous range\nfrom each input vector and writes to a contiguous range of the output vector.</p><p>However, actual use caes for pointwise operators may be more complicated.</p>", "a[href=\"#number-of-outputs\"]": "<h3 class=\"tippy-header\" style=\"margin-top: 0;\">5.4 Number of outputs<a class=\"headerlink\" href=\"#number-of-outputs\" title=\"Link to this heading\">#</a></h3><p>For pointwise operations with multiple output tensors, we need to inform <code class=\"docutils literal notranslate\"><span class=\"pre\">pointwise_dynamic</span></code>\nabout the number of outputs so it could generate code to store the output tensors.\nFor number of inputs, it can be inferred from the length of <code class=\"docutils literal notranslate\"><span class=\"pre\">is_tensor</span></code> of <code class=\"docutils literal notranslate\"><span class=\"pre\">dtypes</span></code>.</p>", "a[href=\"#caching-and-dispatching\"]": "<h2 class=\"tippy-header\" style=\"margin-top: 0;\">4. Caching and dispatching<a class=\"headerlink\" href=\"#caching-and-dispatching\" title=\"Link to this heading\">#</a></h2><p>The decorator <code class=\"docutils literal notranslate\"><span class=\"pre\">@pointwise_dynamic</span></code> returns a <code class=\"docutils literal notranslate\"><span class=\"pre\">PointwiseDynamicFunction</span></code> object,\nwhich servers as the proxy to all the decorated function.\nIt caches call the generated python modules and dispatches to them.</p><p>The dispatch result depends only on the rank of the task-space, rather than the shape of the task-space.</p>", "a[href=\"#pointwise-operations\"]": "<h2 class=\"tippy-header\" style=\"margin-top: 0;\">1. Pointwise operations<a class=\"headerlink\" href=\"#pointwise-operations\" title=\"Link to this heading\">#</a></h2><p>Pointwise operators are trivial to parallelize.\nMost parallel programming guides begin with pointwise addition\nbetween 2 contiguous vectors.\nFor <a class=\"reference external\" href=\"https://triton-lang.org/main/getting-started/tutorials/01-vector-add.html#sphx-glr-getting-started-tutorials-01-vector-add-py\"><code class=\"docutils literal notranslate\"><span class=\"pre\">vector_add</span></code> in Triton</a>,\nit is simple to implement a task partitioning schema that each CTA reads a contiguous range\nfrom each input vector and writes to a contiguous range of the output vector.</p><p>However, actual use caes for pointwise operators may be more complicated.</p>", "a[href=\"#code-generation\"]": "<h2 class=\"tippy-header\" style=\"margin-top: 0;\">2. Code generation<a class=\"headerlink\" href=\"#code-generation\" title=\"Link to this heading\">#</a></h2><p>The basic usage of <code class=\"docutils literal notranslate\"><span class=\"pre\">pointwise_dynamic</span></code> is using it to decorate a <code class=\"docutils literal notranslate\"><span class=\"pre\">triton.jit</span></code> function\nthat has return value, which is used to map inputs to outputs.\nThe JIT function is similar to a function with <code class=\"docutils literal notranslate\"><span class=\"pre\">__device__</span></code> declaration specifier,\na function that can be called from device.\nAnd we generate a triton jit function to call it, which acts like a CUDA kernel\n(a function with <code class=\"docutils literal notranslate\"><span class=\"pre\">__global__</span></code> declaration specifier) that loads and stores data at global memory.</p><p>In order to support input tensors of different ranks, shapes and/or  strides,\nwe pass the shape of the output tensor (which is also the task-space for pointwise operation) ,\nand the strides of each tensor at every dimension.\nThe shape and strides are unpacked and passed to kernel as integers.\nDue to the lack of support for tuple as arguments to Triton kernels,\nwe have to generate different kernels for different number of integers in the shape and strides.\nAlthough Triton supports tuple as arguments since version 3.3, it does not support all operations on tuples\nsuch as indexing, iteration and so on.</p>", "a[href=\"#metadata-computation\"]": "<h2 class=\"tippy-header\" style=\"margin-top: 0;\">3. Metadata Computation<a class=\"headerlink\" href=\"#metadata-computation\" title=\"Link to this heading\">#</a></h2><p>Since pointwise operators shares similar logic at meta data computation,\nwhich has been implemented as a common function used by all <code class=\"docutils literal notranslate\"><span class=\"pre\">PointwiseDynamicFunction</span></code>s.\nIt involes:</p>", "a[href=\"#id1\"]": "<h3 class=\"tippy-header\" style=\"margin-top: 0;\">6.1 Basic<a class=\"headerlink\" href=\"#id1\" title=\"Link to this heading\">#</a></h3><p><code class=\"docutils literal notranslate\"><span class=\"pre\">PointwiseDynamicFunction</span></code> can be called with the same function signature as the decorated function,\nas shown in previous examples.</p>", "a[href=\"#use-pointwisedynamicfunction\"]": "<h2 class=\"tippy-header\" style=\"margin-top: 0;\">6. Use <code class=\"docutils literal notranslate\"><span class=\"pre\">PointwiseDynamicFunction</span></code><a class=\"headerlink\" href=\"#use-pointwisedynamicfunction\" title=\"Link to this heading\">#</a></h2><h3>6.1 Basic<a class=\"headerlink\" href=\"#id1\" title=\"Link to this heading\">#</a></h3><p><code class=\"docutils literal notranslate\"><span class=\"pre\">PointwiseDynamicFunction</span></code> can be called with the same function signature as the decorated function,\nas shown in previous examples.</p>", "a[href=\"#tensor-non-tensor\"]": "<h3 class=\"tippy-header\" style=\"margin-top: 0;\">5.2 Tensor/Non-Tensor<a class=\"headerlink\" href=\"#tensor-non-tensor\" title=\"Link to this heading\">#</a></h3><p>By default, <code class=\"docutils literal notranslate\"><span class=\"pre\">@pointwise_dynamic</span></code> treats each arguemnt as tensor, and generates code to load/store them.\nBut it can be configured by passing a list of boolean values to the parameter <code class=\"docutils literal notranslate\"><span class=\"pre\">is_tensor</span></code>\nto indicate whether the corresponding argument is tensor or nor.</p><p>For non-tensor arguments, its type can be specfied by passing <code class=\"docutils literal notranslate\"><span class=\"pre\">dtypes</span></code> to the decorator, although not required.\nFor tensor arguments, the corresponding value in <code class=\"docutils literal notranslate\"><span class=\"pre\">dtypes</span></code> is ignored, since its dtype is dynamic\nand Triton can dispatch according to it.</p>", "a[href=\"#in-place-operation-output-arguments\"]": "<h3 class=\"tippy-header\" style=\"margin-top: 0;\">6.2 In-place Operation &amp; Output arguments<a class=\"headerlink\" href=\"#in-place-operation-output-arguments\" title=\"Link to this heading\">#</a></h3><p>Since <code class=\"docutils literal notranslate\"><span class=\"pre\">@pointwise_dynamic</span></code> generates wrappers that take outputs as arguments,\nwe can use it to implement inplace-operations.\nFor all <code class=\"docutils literal notranslate\"><span class=\"pre\">PointwiseDynamicFunction</span></code>s, you can pass output parameters to it using keyword arguments.\nTo discriminate between input arguments and output arguments, we follow a simple rule that\nall input arguments must be passed by position and all output arguments must be passed\nusing keyword arguments.</p><p>The output parameters are named as <code class=\"docutils literal notranslate\"><span class=\"pre\">out{output_index}</span></code>.\nSince the decorated function does not have name for return values,\nwe simply use the naming rule by suffixing <code class=\"docutils literal notranslate\"><span class=\"pre\">out</span></code> with output index.</p>", "a[href=\"#basic\"]": "<h3 class=\"tippy-header\" style=\"margin-top: 0;\">5.1 Basic<a class=\"headerlink\" href=\"#basic\" title=\"Link to this heading\">#</a></h3><p>Decorating the pointwise operator function with <code class=\"docutils literal notranslate\"><span class=\"pre\">pointwise_dynamic</span></code> can save the manual handling of\ntensor addressing, tensor read/write, parallel tiling, tensor broadcasting, dynamic dimensions,\nnon-contiguous storage, type promotion, etc.</p><p>For example, in the following code, you only need to provide a Triton JIT function\ndescribing the computational logic (the Payload), the decorated function can then\ntake torch tesors as inputs and outputs, and support broadcasting, type-promotion, etc.</p>", "a[href=\"#manual-instantiation\"]": "<h3 class=\"tippy-header\" style=\"margin-top: 0;\">6.3 Manual Instantiation<a class=\"headerlink\" href=\"#manual-instantiation\" title=\"Link to this heading\">#</a></h3><p>For some operations you may want to skip the metadata computation, especially the process\nto reduce the rank of task space, and prepare all inputs and outputs manually.\nYou can call the <code class=\"docutils literal notranslate\"><span class=\"pre\">instantiate()</span></code> method of <code class=\"docutils literal notranslate\"><span class=\"pre\">PointwiseDynamicFunction</span></code> with a specific task rank\nto get a specific cached function and call it directly.</p><p>For example, the <code class=\"docutils literal notranslate\"><span class=\"pre\">flip</span></code> operator is not a pointwise operator in the sense that\neach element in the output only depends on the element in the inputs at the corresponding position.\nBut if we can create a view of the input tensor with negative strides and shifted data pointer,\nit can be framed as a pointwise copy. That is how we implement it with <code class=\"docutils literal notranslate\"><span class=\"pre\">pointwise_dynamic</span></code>.</p>", "a[href=\"#output-dtypes\"]": "<h3 class=\"tippy-header\" style=\"margin-top: 0;\">5.3 Output dtypes<a class=\"headerlink\" href=\"#output-dtypes\" title=\"Link to this heading\">#</a></h3><p>For pointwise operators to allocate outputs with correct dtype, <code class=\"docutils literal notranslate\"><span class=\"pre\">promotion_methods</span></code> is required.\nSince the output dtype may be depedent on the input dtypes with some rules,\nspecifying the rule is more expressive than providing output dtypes directly.</p><p><code class=\"docutils literal notranslate\"><span class=\"pre\">promotion_methods</span></code> is a list of tuples (one per output), each of which consists of\nseveral argument indices and a promotion method.\nAn argument index (an integer) is used to indicate the position of the argument,\nwhich is dependent by the promotion method.\nThe promotion method (an enum or string) denotes the method of type promotion.</p>"}
skip_classes = ["headerlink", "sd-stretched-link"]

window.onload = function () {
    for (const [select, tip_html] of Object.entries(selector_to_html)) {
        const links = document.querySelectorAll(` ${select}`);
        for (const link of links) {
            if (skip_classes.some(c => link.classList.contains(c))) {
                continue;
            }

            tippy(link, {
                content: tip_html,
                allowHTML: true,
                arrow: true,
                placement: 'auto-start', maxWidth: 500, interactive: false,

            });
        };
    };
    console.log("tippy tips loaded!");
};
