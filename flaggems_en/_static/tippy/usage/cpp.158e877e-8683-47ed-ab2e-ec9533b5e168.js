selector_to_html = {"a[href=\"#reference-currently-supported-c-wrapped-operators\"]": "<h2 class=\"tippy-header\" style=\"margin-top: 0;\">Reference: Currently supported C+\u00b1wrapped operators<a class=\"headerlink\" href=\"#reference-currently-supported-c-wrapped-operators\" title=\"Link to this heading\">#</a></h2><p>The following operators currently have C++ wrappers shipped with <em>FlagGems</em>.</p>", "a[href=\"#install-and-enable\"]": "<h2 class=\"tippy-header\" style=\"margin-top: 0;\">2. Install and enable<a class=\"headerlink\" href=\"#install-and-enable\" title=\"Link to this heading\">#</a></h2><p>To make the C++ wrapper <strong>fully effective</strong> you need both of the following:</p>", "a[href=\"#via-aten-direct-replacement-transparent-torch-patching-at-the-dispatcher\"]": "<h3 class=\"tippy-header\" style=\"margin-top: 0;\">3.2 Via ATen direct replacement (transparent <code class=\"docutils literal notranslate\"><span class=\"pre\">torch.*</span></code> patching at the dispatcher)<a class=\"headerlink\" href=\"#via-aten-direct-replacement-transparent-torch-patching-at-the-dispatcher\" title=\"Link to this heading\">#</a></h3><p>For a subset of operators, <em>FlagGems</em> additionally registers the C++\nimplementations directly under the <strong><code class=\"docutils literal notranslate\"><span class=\"pre\">aten::</span></code> namespace</strong> using\n<code class=\"docutils literal notranslate\"><span class=\"pre\">TORCH_LIBRARY_IMPL(aten,</span> <span class=\"pre\">&lt;dispatch_key&gt;,</span> <span class=\"pre\">m)</span></code> in\n<code class=\"docutils literal notranslate\"><span class=\"pre\">src/flag_gems/csrc/aten_patch.cpp</span></code>. The dispatch key is chosen by backend:</p>", "a[href=\"#architecture\"]": "<h2 class=\"tippy-header\" style=\"margin-top: 0;\">1. Architecture<a class=\"headerlink\" href=\"#architecture\" title=\"Link to this heading\">#</a></h2><p>The C++ wrapped operators in <em>FlagGems</em> are built on top of\n<a class=\"reference external\" href=\"https://github.com/flagos-ai/libtriton_jit\"><code class=\"docutils literal notranslate\"><span class=\"pre\">libtriton_jit</span></code></a>, a multi-backend\nC++ runtime for Triton JIT functions. <code class=\"docutils literal notranslate\"><span class=\"pre\">libtriton_jit</span></code> reimplements the Triton\nJIT runtime in C++ (argument specialization, kernel caching, and launch) while\ndelegating the actual compilation to the upstream Triton compiler.</p><p>In this stack:</p>", "a[href=\"#via-the-c-operators-pybind-module-direct-dispatcher-free\"]": "<h3 class=\"tippy-header\" style=\"margin-top: 0;\">3.3 Via the <code class=\"docutils literal notranslate\"><span class=\"pre\">c_operators</span></code> pybind module (direct, dispatcher-free)<a class=\"headerlink\" href=\"#via-the-c-operators-pybind-module-direct-dispatcher-free\" title=\"Link to this heading\">#</a></h3><p>The same C++ wrappers are also exported through a <code class=\"docutils literal notranslate\"><span class=\"pre\">PYBIND11_MODULE(c_operators,</span> <span class=\"pre\">\u2026)</span></code>\nin <code class=\"docutils literal notranslate\"><span class=\"pre\">src/flag_gems/csrc/cstub.cpp</span></code>:</p>", "a[href=\"#using-c-based-operators-for-optimal-performance\"]": "<h1 class=\"tippy-header\" style=\"margin-top: 0;\">Using C+\u00b1Based Operators for Optimal Performance<a class=\"headerlink\" href=\"#using-c-based-operators-for-optimal-performance\" title=\"Link to this heading\">#</a></h1><p>Another advanced optimization path with <em>FlagGems</em> is the use of <em>C++ wrapped operators</em>\nfor selected operations. While Triton kernels offer reasonably good compute performance,\nTriton itself is a DSL implemented in Python. This means that both the operator definitions and\nthe runtime dispatchers are written in Python, which can introduce <strong>non-trivial overhead</strong>\nin latency-sensitive or high-throughput scenarios.</p><p>To address this, <em>FlagGems</em> provides a C++ runtime solution that encapsulates\nthe operator\u2019s wrapper logic, registration mechanism, and runtime management in C++,\nwhile still reusing the underlying Triton kernels for the actual computation.\nThis approach preserves the kernel-level efficiency from Triton\nwhile significantly reducing Python-related overhead, enabling tighter integration\nwith low-level CUDA workflows and improving overall inference performance.</p>", "a[href=\"#via-the-native-c-api-flag-gems-functions-and-gtest\"]": "<h3 class=\"tippy-header\" style=\"margin-top: 0;\">3.4 Via the native C++ API (<code class=\"docutils literal notranslate\"><span class=\"pre\">flag_gems::</span></code> functions and GTest)<a class=\"headerlink\" href=\"#via-the-native-c-api-flag-gems-functions-and-gtest\" title=\"Link to this heading\">#</a></h3><p>Every wrapper is also a regular C++ function in the <code class=\"docutils literal notranslate\"><span class=\"pre\">flag_gems::</span></code>\nnamespace, declared in <code class=\"docutils literal notranslate\"><span class=\"pre\">include/flag_gems/operators.h</span></code> and shipped in the\ninstalled CMake target <code class=\"docutils literal notranslate\"><span class=\"pre\">FlagGems::operators</span></code>. Downstream C++ code can link\nagainst this target and call the operators directly:</p>", "a[href=\"#ways-to-invoke-c-operators\"]": "<h2 class=\"tippy-header\" style=\"margin-top: 0;\">3. Ways to invoke C++ operators<a class=\"headerlink\" href=\"#ways-to-invoke-c-operators\" title=\"Link to this heading\">#</a></h2><p>Once the C++ extensions are built, the same underlying C++ wrapper can be\ninvoked through <strong>four</strong> different paths. Each path targets a different use\ncase and has a different level of dispatcher overhead.</p>", "a[href=\"#via-torch-ops-flag-gems-custom-op-namespace\"]": "<h3 class=\"tippy-header\" style=\"margin-top: 0;\">3.1 Via <code class=\"docutils literal notranslate\"><span class=\"pre\">torch.ops.flag_gems.*</span></code> (custom-op namespace)<a class=\"headerlink\" href=\"#via-torch-ops-flag-gems-custom-op-namespace\" title=\"Link to this heading\">#</a></h3><p>All C++ wrappers are registered as PyTorch <em>custom ops</em> under the\n<code class=\"docutils literal notranslate\"><span class=\"pre\">flag_gems</span></code> namespace via <code class=\"docutils literal notranslate\"><span class=\"pre\">TORCH_LIBRARY(flag_gems,</span> <span class=\"pre\">m)</span></code> in\n<code class=\"docutils literal notranslate\"><span class=\"pre\">src/flag_gems/csrc/cstub.cpp</span></code>. You can call them explicitly from Python,\nbypassing any patching logic or Python-side fall back paths:</p>", "a[href=\"#summary\"]": "<h3 class=\"tippy-header\" style=\"margin-top: 0;\">Summary<a class=\"headerlink\" href=\"#summary\" title=\"Link to this heading\">#</a></h3>"}
skip_classes = ["headerlink", "sd-stretched-link"]

window.onload = function () {
    for (const [select, tip_html] of Object.entries(selector_to_html)) {
        const links = document.querySelectorAll(` ${select}`);
        for (const link of links) {
            if (skip_classes.some(c => link.classList.contains(c))) {
                continue;
            }

            tippy(link, {
                content: tip_html,
                allowHTML: true,
                arrow: true,
                placement: 'auto-start', maxWidth: 500, interactive: false,

            });
        };
    };
    console.log("tippy tips loaded!");
};
